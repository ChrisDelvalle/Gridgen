# Example Jekyll Site Target

This directory is intentionally minimal. Use it as a destination for local
Gridgen build output:

```sh
bun run gridgen build examples/sample-grid examples/jekyll-site
```

Generated includes are written under `_includes/gridgen/`, and generated images
and CSS are written under `assets/gridgen/`.
