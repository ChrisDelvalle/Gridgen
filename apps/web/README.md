# @gridgen/web

Vite React authoring UI for Gridgen.

This package owns browser-only editor behavior. It should communicate with the
local Gridgen server through browser-safe DTOs and should not import filesystem,
server, CLI, or image-processing modules directly.
